use std::convert::{TryFrom, TryInto};
use crate::world::v1::v12::{SimpleEnum, SimpleEnumError};
use crate::{ConstantSized, MaximumPossibleSized, ReadableAndWritable, VariableSized};
#[cfg(any(feature = "async_tokio", feature = "async_std"))]
use crate::AsyncReadWrite;
#[cfg(any(feature = "async_tokio", feature = "async_std"))]
use async_trait::async_trait;
#[cfg(feature = "async_tokio")]
use tokio::io::{AsyncReadExt, AsyncWriteExt};

#[derive(Debug, PartialEq, Clone, Default)]
pub struct SimpleIfNotEnumElse {
    pub enum: SimpleIfNotEnumElseSimpleEnum,
}

impl ReadableAndWritable for SimpleIfNotEnumElse {
    type Error = SimpleIfNotEnumElseError;

    fn read<R: std::io::Read>(r: &mut R) -> std::result::Result<Self, Self::Error> {
        // enum: SimpleEnum
        let enum = SimpleEnum::read(r)?;

        let enum_if = match enum {
            SimpleEnum::ZERO => {
                // zero: u16
                let zero = crate::util::read_u16_le(r)?;

                SimpleIfNotEnumElseSimpleEnum::ZERO {
                    zero,
                }
            }
            SimpleEnum::ONE => {
                // others: u8
                let others = crate::util::read_u8_le(r)?;

                SimpleIfNotEnumElseSimpleEnum::ONE {
                    others,
                }
            }
            SimpleEnum::TWO => {
                // others: u8
                let others = crate::util::read_u8_le(r)?;

                SimpleIfNotEnumElseSimpleEnum::TWO {
                    others,
                }
            }
        };

        Ok(Self {
            enum: enum_if,
        })
    }

    fn write<W: std::io::Write>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        // enum: SimpleEnum
        self.enum.write(w)?;

        match &self.enum {
            SimpleIfNotEnumElseSimpleEnum::ZERO {
                zero,
            } => {
                // zero: u16
                w.write_all(&zero.to_le_bytes())?;

            }
            SimpleIfNotEnumElseSimpleEnum::ONE {
                others,
            } => {
                // others: u8
                w.write_all(&others.to_le_bytes())?;

            }
            SimpleIfNotEnumElseSimpleEnum::TWO {
                others,
            } => {
                // others: u8
                w.write_all(&others.to_le_bytes())?;

            }
        }

        Ok(())
    }

}

#[cfg(any(feature = "async_tokio", feature = "async_std"))]
#[async_trait]
impl AsyncReadWrite for SimpleIfNotEnumElse {
    type Error = SimpleIfNotEnumElseError;

    #[cfg(feature = "async_tokio")]
    async fn tokio_read<R: AsyncReadExt + Unpin + Send>(r: &mut R) -> std::result::Result<Self, Self::Error> {
        // enum: SimpleEnum
        let enum = SimpleEnum::tokio_read(r).await?;

        let enum_if = match enum {
            SimpleEnum::ZERO => {
                // zero: u16
                let zero = crate::util::tokio_read_u16_le(r).await?;

                SimpleIfNotEnumElseSimpleEnum::ZERO {
                    zero,
                }
            }
            SimpleEnum::ONE => {
                // others: u8
                let others = crate::util::tokio_read_u8_le(r).await?;

                SimpleIfNotEnumElseSimpleEnum::ONE {
                    others,
                }
            }
            SimpleEnum::TWO => {
                // others: u8
                let others = crate::util::tokio_read_u8_le(r).await?;

                SimpleIfNotEnumElseSimpleEnum::TWO {
                    others,
                }
            }
        };

        Ok(Self {
            enum: enum_if,
        })
    }

    #[cfg(feature = "async_tokio")]
    async fn tokio_write<W: AsyncWriteExt + Unpin + Send>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        // enum: SimpleEnum
        self.enum.tokio_write(w).await?;

        match &self.enum {
            SimpleIfNotEnumElseSimpleEnum::ZERO {
                zero,
            } => {
                // zero: u16
                w.write_all(&zero.to_le_bytes()).await?;

            }
            SimpleIfNotEnumElseSimpleEnum::ONE {
                others,
            } => {
                // others: u8
                w.write_all(&others.to_le_bytes()).await?;

            }
            SimpleIfNotEnumElseSimpleEnum::TWO {
                others,
            } => {
                // others: u8
                w.write_all(&others.to_le_bytes()).await?;

            }
        }

        Ok(())
    }

}

impl VariableSized for SimpleIfNotEnumElse {
    fn size(&self) -> usize {
        self.enum.size() // enum: SimpleEnum and subfields
    }
}

impl MaximumPossibleSized for SimpleIfNotEnumElse {
    fn maximum_possible_size() -> usize {
        SimpleEnum::maximum_possible_size() // enum: SimpleEnum
    }
}

#[derive(Debug)]
pub enum SimpleIfNotEnumElseError {
    Io(std::io::Error),
    SimpleEnum(SimpleEnumError),
}

impl std::error::Error for SimpleIfNotEnumElseError {}
impl std::fmt::Display for SimpleIfNotEnumElseError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Io(i) => i.fmt(f),
            Self::SimpleEnum(i) => i.fmt(f),
        }
    }
}

impl From<std::io::Error> for SimpleIfNotEnumElseError {
    fn from(e : std::io::Error) -> Self {
        Self::Io(e)
    }
}

impl From<SimpleEnumError> for SimpleIfNotEnumElseError {
    fn from(e: SimpleEnumError) -> Self {
        Self::SimpleEnum(e)
    }
}

#[derive(Debug, PartialEq, Clone)]
pub enum SimpleIfNotEnumElseSimpleEnum {
    ZERO {
        zero: u16,
    },
    ONE {
        others: u8,
    },
    TWO {
        others: u8,
    },
}

impl From<&SimpleEnum> for SimpleIfNotEnumElseSimpleEnum {
    fn from(e: &SimpleEnum) -> Self {
        match &e {
            SimpleEnum::ZERO => Self::ZERO {
                zero: Default::default(),
            },
            SimpleEnum::ONE => Self::ONE {
                others: Default::default(),
            },
            SimpleEnum::TWO => Self::TWO {
                others: Default::default(),
            },
        }
    }
}

impl From<&SimpleIfNotEnumElseSimpleEnum> for SimpleEnum {
    fn from(v: &SimpleIfNotEnumElseSimpleEnum) -> Self {
        match &v {
            SimpleIfNotEnumElseSimpleEnum::ZERO { .. } => Self::ZERO,
            SimpleIfNotEnumElseSimpleEnum::ONE { .. } => Self::ONE,
            SimpleIfNotEnumElseSimpleEnum::TWO { .. } => Self::TWO,
        }
    }
}

impl Default for SimpleIfNotEnumElseSimpleEnum {
    fn default() -> Self {
        // First enumerator without any fields
        Self::ZERO {
            zero: Default::default(),
        }
    }
}

impl SimpleIfNotEnumElseSimpleEnum {
    pub fn write<W: std::io::Write>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        let a: SimpleEnum = self.into();
        a.write(w)?;
        Ok(())
    }

    #[cfg(feature = "async_tokio")]
    pub async fn tokio_write<W: AsyncWriteExt + Unpin + Send>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        let a: SimpleEnum = self.into();
        a.tokio_write(w).await?;
        Ok(())
    }

    pub fn write_u16_le<W: std::io::Write>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        let a: SimpleEnum = self.into();
        a.write_u16_le(w)
    }

    #[cfg(feature = "async_tokio")]
    pub async fn tokio_write_u16_le<W: AsyncWriteExt + Unpin + Send>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        let a: SimpleEnum = self.into();
        a.tokio_write_u16_le(w).await
    }

    pub fn write_u16_be<W: std::io::Write>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        let a: SimpleEnum = self.into();
        a.write_u16_be(w)
    }

    #[cfg(feature = "async_tokio")]
    pub async fn tokio_write_u16_be<W: AsyncWriteExt + Unpin + Send>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        let a: SimpleEnum = self.into();
        a.tokio_write_u16_be(w).await
    }

    pub fn write_u32_le<W: std::io::Write>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        let a: SimpleEnum = self.into();
        a.write_u32_le(w)
    }

    #[cfg(feature = "async_tokio")]
    pub async fn tokio_write_u32_le<W: AsyncWriteExt + Unpin + Send>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        let a: SimpleEnum = self.into();
        a.tokio_write_u32_le(w).await
    }

    pub fn write_u32_be<W: std::io::Write>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        let a: SimpleEnum = self.into();
        a.write_u32_be(w)
    }

    #[cfg(feature = "async_tokio")]
    pub async fn tokio_write_u32_be<W: AsyncWriteExt + Unpin + Send>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        let a: SimpleEnum = self.into();
        a.tokio_write_u32_be(w).await
    }

    pub fn write_u64_le<W: std::io::Write>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        let a: SimpleEnum = self.into();
        a.write_u64_le(w)
    }

    #[cfg(feature = "async_tokio")]
    pub async fn tokio_write_u64_le<W: AsyncWriteExt + Unpin + Send>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        let a: SimpleEnum = self.into();
        a.tokio_write_u64_le(w).await
    }

    pub fn write_u64_be<W: std::io::Write>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        let a: SimpleEnum = self.into();
        a.write_u64_be(w)
    }

    #[cfg(feature = "async_tokio")]
    pub async fn tokio_write_u64_be<W: AsyncWriteExt + Unpin + Send>(&self, w: &mut W) -> std::result::Result<(), std::io::Error> {
        let a: SimpleEnum = self.into();
        a.tokio_write_u64_be(w).await
    }

}

impl VariableSized for SimpleIfNotEnumElseSimpleEnum {
    fn size(&self) -> usize {
        match self {
            Self::ZERO  {
                zero,
            } => {
                1
                + 2 // zero: u16
            }
            Self::ONE  {
                others,
            } => {
                1
                + 1 // others: u8
            }
            Self::TWO  {
                others,
            } => {
                1
                + 1 // others: u8
            }
        }
    }
}

impl MaximumPossibleSized for SimpleIfNotEnumElseSimpleEnum {
    fn maximum_possible_size() -> usize {
        65536 // maximum possible u16 size. TODO value.
    }
}

